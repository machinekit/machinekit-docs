step1: filter out everything except man/* and move man up one level:

this commit is the result post-applying
     git filter-branch --subdirectory-filter man -- --all
see also:
http://stackoverflow.com/questions/1662753/export-subtree-in-git-with-history

step2: move the subtree to docs/man

this is now ready for importing in machinekit-docs under docs/man

step3: add default frontmatter

